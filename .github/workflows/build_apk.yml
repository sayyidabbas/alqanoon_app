Name: Build Android APK

on:
  push:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    # تم التحديث إلى v4 لإزالة التحذيرات
    - name: Checkout Repository
      uses: actions/checkout@v4

    # تم التحديث إلى v4 لإزالة التحذيرات
    - name: Set up Java
      uses: actions/setup-java@v4
      with:
        distribution: 'zulu'
        java-version: '17'

    - name: Set up Flutter
      uses: subosito/flutter-action@v2
      with:
        flutter-version: '3.x'
        channel: 'stable'

    - name: Recreate Android Project Files
      run: flutter create . --platforms=android

    - name: Force compileSdk 36 globally
      run: |
        echo "
        subprojects {
            afterEvaluate { project ->
                if (project.hasProperty('android')) {
                    project.android {
                        compileSdk = 36
                        compileSdkVersion 36
                    }
                }
            }
        }
        " >> android/build.gradle

    - name: Install Dependencies
      run: flutter pub get

    - name: Copy Firebase Config
      run: cp google-services.json android/app/

    - name: Set App Name
      run: flutter pub run rename setAppName --targets android --value "منصة القانون"

    - name: Build APK (Bypass Flutter CLI Check)
      run: flutter build apk --release || true

    - name: Find and Upload APK
      # تم التحديث إلى v4
      uses: actions/upload-artifact@v4
      with:
        name: law-platform-apk
        # سيبحث الخادم عن أي ملف بصيغة apk في جميع مجلدات المشروع بلا استثناء
        path: '**/*.apk'


تفضل الكود
